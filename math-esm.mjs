//ES modules

const add = (a, b) => {
  return a + b;
};

// export default add

// export default (a,b)=>{
//     return a+b;
// }

const subtract = (a, b) => {
  return a - b;
};

export default {
  add,
  subtract,
};
